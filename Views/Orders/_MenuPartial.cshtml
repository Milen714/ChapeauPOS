@model ChapeauPOS.Models.ViewModels.MenuViewModel


<div id="courseSidebar">
    <button onclick="filterCourse('starter')">Starter</button>
    <button onclick="filterCourse('main')">Main</button>
    <button onclick="filterCourse('dessert')">Dessert</button>
    <button onclick="filterCourse('drink')">Drinks</button>
</div>

<div id="menuItemsContainer">
    @foreach (var item in Model.Starters)
    {
       
            <form class="menu-item-form" method="post">
            <input class="item-id" type="hidden" name="itemId" value="@item.MenuItemID" />
            <div class="menu-item square clickable-item" data-course="starter">
                    <h4>@item.ItemName</h4>
                    <p>@item.ItemPrice.ToString("C")</p>
                </div>
            </form>
    }
    @foreach (var item in Model.MainCourses)
    {
        <form class="menu-item-form" method="post">
            <input class="item-id" type="hidden" name="itemId" value="@item.MenuItemID" />
            <div class="menu-item square clickable-item" data-course="main">
                <h4>@item.ItemName</h4>
                <p>@item.ItemPrice.ToString("C")</p>
            </div>
        </form>
    }
    @foreach (var item in Model.Desserts)
    {
        <form class="menu-item-form" method="post">
            <input class="item-id" type="hidden" name="itemId" value="@item.MenuItemID" />
            <div class="menu-item square clickable-item" data-course="dessert">
                <h4>@item.ItemName</h4>
                <p>@item.ItemPrice.ToString("C")</p>
            </div>
        </form>
    }
    @foreach (var item in Model.Drinks)
    {
        <form class="menu-item-form" method="post">
            <input class="item-id" type="hidden" name="itemId" value="@item.MenuItemID" />
            <div class="menu-item square clickable-item" data-course="drink">
                <h4>@item.ItemName</h4>
                <p>@item.ItemPrice.ToString("C")</p>
            </div>
        </form>
    }
</div>

