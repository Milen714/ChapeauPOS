## Environment Setup

Create a `.env` file in the project root with the following:

```env
# SQL Server connection string
ChapeauDB_CONNECTION=Server=<SERVER_NAME>;Database=<DB_NAME>;User Id=<USERNAME>;Password=<PASSWORD>;TrustServerCertificate=True
